{
  "name": "zip-lists",
  "main": "index.js",
  "scripts": {
    "build": "../../node_modules/.binrollup -c -w",
    "serve": "live-server . --port=8080 --no-browser",
    "go": "run-p build serve"
  }
}
